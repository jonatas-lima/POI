apiVersion: v1
kind: Service
metadata:
  labels:
    app: app
  name: app
spec:
  ports:
  - name: 3000-3000
    nodePort: 30000
    port: 3000
    protocol: TCP
    targetPort: 3000
  selector:
    app: app
  type: NodePort
status:
  loadBalancer: {}
